#include<stdio.h>
#include "test.h"

// char *reverse(char *str) {
//     int len = strlen(str);
//     char *rts = (char *) malloc(len + 1);
//     for (int i = 0; i < len; i++) {
//       rts[i] = str[len - i - 1];
//     }
//     rts[len] = '\0';
//     return rts;
// }

int main() {
    // char s[] = "CS2110";
    // printf("%s", reverse(&s));
    // struct a {
    // int   a1;
    // short a2;
    // char  a3[8]; 
    // };
    // struct a b[11];
    // char *str = "meta";

    // printf("%d", sizeof(b[3].a3));

    // char a[] = "mateys";
    // int b=6;
    // char c='8';
    // double d=5.4,e=3.2;
    // char *pa = a;
    // char **ppa = &pa;
    // int *pb = &b;
    // int **ppb = &pb;
    // char *pc = &c;
    // char **ppc = &pc;
    // double *pd = &d, *pe = &e;
    // double **ppd = &pd, **ppe = &pe;

    // *pb = *pb/2;
    // a[b] = '3';
    // printf("%s", a);
    // pe = &d;
    // ppd = &pe;
    // printf("\n%f", **ppd);
    // *pe = *pe + 1.8;
    // printf("\n%f", *pe);
    // *pc = *(pa + (int)(d-e));
    // printf("\n%c", *pc);
    // a[b] = '\0';
    // printf("\n%s", a);
}